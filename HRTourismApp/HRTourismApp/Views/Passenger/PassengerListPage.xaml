<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HRTourismApp.Views.Passenger.PassengerListPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Yeni Yolcu" Command="{Binding AddCommand}"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <ListView x:Name="ListViewPassenger"
                  ItemsSource="{Binding PassengerList}"
                  ItemTapped="ListViewPassenger_ItemTapped" 
                  CachingStrategy="RecycleElement"
                     SeparatorVisibility="None"
        BackgroundColor="{ DynamicResource BasePageColor }"
        HasUnevenRows="true">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell >
                        <ViewCell.View>
                            <Grid >
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                    <RowDefinition Height="0.5"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Label Text="{Binding FirstName}" Grid.Row="0" Grid.Column="0"  FontSize="Large"/>
                                <Label Text="{Binding LastName}" Grid.Row="0" Grid.Column="1"  FontSize="Large" />
                                <Label Text="{Binding Gender}" Grid.Row="0" Grid.Column="2" />
                                <Label Text="{Binding CountryCode}" Grid.Row="1" Grid.Column="0" FontAttributes="Italic"/>
                                <Label Text="{Binding DocumentNo}" Grid.Row="1" Grid.Column="1" FontAttributes="Italic"/>
                                <Label Text="{Binding Phone}" Grid.Row="1" Grid.Column="2" FontAttributes="Italic"/>

                                <BoxView Grid.Row="2"
                             Grid.ColumnSpan="3"                             
                             BackgroundColor="LightGray"/>

                            </Grid>
                        </ViewCell.View>
                    </ViewCell>                   
                   
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </ContentPage.Content>

</ContentPage>


